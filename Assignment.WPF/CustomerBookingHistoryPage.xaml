<Page x:Class="Assignment.WPF.CustomerBookingHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Assignment.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CustomerBookingHistoryPage">

   

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="10" 
                   HorizontalAlignment="Center" 
                   FontSize="22" 
                   FontWeight="SemiBold"
                   Foreground="#007ACC" 
                   Text="LỊCH SỬ ĐẶT PHÒNG CỦA TÔI"/>

        <DataGrid Grid.Row="1" Name="Reservation" 
                  CanUserAddRows="False" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  SelectionMode="Single" 
                  Margin="0, 10, 0, 0"
                  MouseDoubleClick="Reservation_OnMouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding BookingReservationId}" Width="*"/>
                <DataGridTextColumn Header="Ngày Đặt" Binding="{Binding BookingDate, StringFormat='dd/MM/yyyy'}" Width="2*"/>
                <DataGridTextColumn Header="Tổng Giá" Binding="{Binding TotalPrice, StringFormat=C0}" Width="2*"/>

                <DataGridTextColumn Header="Trạng Thái" Width="1.5*">
                    <DataGridTextColumn.Binding>
                        <Binding Path="BookingStatus" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>

               

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>