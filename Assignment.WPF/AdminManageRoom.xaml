<Page x:Class="Assignment.WPF.AdminManageRoom"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="AdminManageRoom"
      Background="#F8F8F8">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="QUẢN LÝ THÔNG TIN PHÒNG"
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="#007ACC"
                   HorizontalAlignment="Center"
                   Margin="0, 10, 0, 15"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3.5*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Name="Rooms" 
                      CanUserAddRows="False" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      SelectionMode="Single"
                      Margin="0, 0, 15, 0"
                      PreviewMouseLeftButtonUp="Rooms_OnPreviewMouseLeftButtonUp">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding RoomId}" Width="30"/>
                    <DataGridTextColumn Header="Số Phòng" Binding="{Binding RoomNumber}" Width="*"/>
                    <DataGridTextColumn Header="Loại Phòng" Binding="{Binding RoomType.RoomTypeName}" Width="2*"/>
                    <DataGridTextColumn Header="Sức Chứa" Binding="{Binding RoomMaxCapacity}" Width="*"/>
                    <DataGridTextColumn Header="Giá/Ngày" Binding="{Binding RoomPricePerDay, StringFormat=C0}" Width="2*"/>
                    <DataGridTextColumn Header="Mô Tả Chi tiết" Binding="{Binding RoomDetailDescription}" Width="5*"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding RoomStatus}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10, 0">

                    <TextBlock Text="Room Id" />
                    <TextBox Name="txtId" IsReadOnly="True" Text="{Binding ElementName=Rooms, Path=SelectedItem.RoomId, Mode=OneWay}"  />

                    <TextBlock Text="Room Number" />
                    <TextBox Name="txtNumber" Text="{Binding ElementName=Rooms, Path=SelectedItem.RoomNumber, Mode=OneWay}"  />

                    <TextBlock Text="Maximum Capacity" />
                    <TextBox Name="txtMaxCapacity" Text="{Binding ElementName=Rooms, Path=SelectedItem.RoomMaxCapacity, Mode=OneWay}"  />

                    <TextBlock Text="Room Type" />
                    <ComboBox Name="cbRoomType" SelectionChanged="CbRoomType_OnSelectionChanged" ></ComboBox>

                    <TextBox Name="txtRoomType" Margin="0, 5, 0, 10" Height="80" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" ></TextBox>

                    <TextBlock Text="Status" />
                    <StackPanel Orientation="Horizontal" Margin="0, 5, 0, 10">
                        <RadioButton Content="Active" Margin="0,0,20,0" Name="active" GroupName="RoomStatus"></RadioButton>
                        <RadioButton Content="Deactive" Name="deactive" GroupName="RoomStatus"></RadioButton>
                    </StackPanel>

                    <TextBlock Text="Price per Day" />
                    <TextBox Name="txtPrice" Text="{Binding ElementName=Rooms, Path=SelectedItem.RoomPricePerDay, Mode=OneWay}"  />

                    <TextBlock Text="Room Description" />
                    <TextBox Name="txtDescription" 
                             Text="{Binding ElementName=Rooms, Path=SelectedItem.RoomDetailDescription, Mode=OneWay}" 
                             TextWrapping="Wrap" 
                             AcceptsReturn="True"
                             Height="60"
                             VerticalScrollBarVisibility="Auto"
                             />

                    <Grid Margin="0, 20, 0, 10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Refresh" Grid.Column="0" Click="ButtonBase_OnClick"  Margin="5"/>
                        <Button Content="Add New" Grid.Column="1" Click="ButtonBase_OnClickAdd"  Margin="5"/>
                    </Grid>

                    <Grid Margin="0, 5, 0, 10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Update" Grid.Column="0" Click="ButtonBase_OnClickAdd"  Margin="5"/>
                        <Button Content="Delete (Deactivate)" Grid.Column="1" Click="ButtonBase_OnClickDelete"  Margin="5"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>

    <Page.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0, 10, 0, 3"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5, 0, 5, 0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InputBoxReadOnlyStyle" TargetType="TextBox" BasedOn="{StaticResource InputBoxStyle}">
            <Setter Property="Background" Value="#EAEAEA"/>
            <Setter Property="Foreground" Value="#666666"/>
        </Style>

        <Style x:Key="InputBoxMultiLineStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="InputBoxReadOnlyMultiLineStyle" TargetType="TextBox" BasedOn="{StaticResource InputBoxMultiLineStyle}">
            <Setter Property="Background" Value="#EAEAEA"/>
            <Setter Property="Foreground" Value="#666666"/>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#CCCCCC"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

    </Page.Resources>
</Page>